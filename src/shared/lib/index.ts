// Utilitarios puros e neutros de dominio (formatadores, helpers, etc.).
export {
  firebaseApp,
  auth,
  firestore,
  firebaseAI,
  appCheck,
  getFirebaseApp,
  getFirebaseAI,
  getAppCheck,
} from './firebase';
