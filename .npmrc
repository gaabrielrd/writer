# Recusa a instalação quando a versão do Node não satisfaz "engines" no
# package.json. Sem isso, a exigência de Node >= 22 é apenas informativa
# e o projeto quebra mais tarde, em erro de sintaxe ou de API ausente.
engine-strict=true
